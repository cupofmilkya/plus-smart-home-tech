spring:
  kafka:
    bootstrap-servers: localhost:9092

    producer:
      properties:
        enable-idempotence: false
        acks: all

logging:
  level:
    root: INFO
    ru.practicum: DEBUG
    ru.practicum.kafka: DEBUG
    ru.practicum.kafka.serialization: DEBUG
    org.springframework.kafka: INFO
    org.apache.kafka: INFO